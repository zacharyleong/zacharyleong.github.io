/**
 * Header layout breakpoints, in CSS pixels.
 *
 * Change these three values to tune every responsive header change together:
 * logo size/orientation, navigation variant, and spacing.
 */
export const HEADER_BREAKPOINTS = {
  smallHorizontal: 420,
  large: 798,
  largeHorizontal: 950,
} as const;
